{
  "field_access_pattern": "classic",
  "processors": [
    {
      "pipeline": {
        "name": "logs-windows.sysmon_operational",
        "ignore_missing_pipeline": true,
        "ignore_failure": true,
        "if": "ctx.wincode != null || ctx.eventcategory != null"
      }
    }
  ]
}
