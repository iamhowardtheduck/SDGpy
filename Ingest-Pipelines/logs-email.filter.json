{
  "field_access_pattern": "classic",
  "processors": [
    {
      "pipeline": {
        "name": "enrich-email",
        "ignore_missing_pipeline": true,
        "ignore_failure": true,
        "if": "ctx.fname != null || ctx.filename != null"
      }
    }
  ]
}
